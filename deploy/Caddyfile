# Caddy fronts only the public API now — the frontend is served by CloudFront + S3.
# Caddy auto-provisions a free Let's Encrypt certificate for API_DOMAIN.
# CORS is handled by the API itself (Cors__AllowedOrigins), not here.

{$API_DOMAIN} {
	encode gzip
	reverse_proxy api:5000
}
